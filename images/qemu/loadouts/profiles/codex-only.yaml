apiVersion: loadout/v1
kind: loadout
metadata:
  name: codex-only
  description: Single-provider VM with OpenAI Codex CLI and SDLC framework
  labels:
    category: per-provider
    complexity: standard

extends:
  - layers/base-dev.yaml
  - layers/docker.yaml
  - layers/databases.yaml
  - providers/codex.yaml

network:
  mode: full

aiwg:
  enabled: true
  frameworks:
    - name: sdlc-complete
      providers:
        - codex
