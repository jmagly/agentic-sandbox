apiVersion: loadout/v1
kind: layer
metadata:
  name: databases
  description: Database client tools

packages:
  - postgresql-client
  - default-mysql-client
  - sqlite3
  # redis-tools excluded: libjemalloc2/liblzf1 deps not resolvable on base image (#137)
  # Install manually if needed: apt-get install redis-tools after base image rebuild
