apiVersion: loadout/v1
kind: layer
metadata:
  name: provider-factory
  description: Factory AI (AIWG provider - droid CLI)

# Factory ships the "droid" CLI via the official installer
# (https://app.factory.ai/cli) (#612).
runtimes:
  node:
    enabled: true
    method: fnm
    version: lts

ai_tools:
  droid:
    enabled: true
