extends:
  - spectral:oas

rules:
  # spectral-cli 6.16.0's enum selectors crash while traversing valid OpenAPI
  # 3.1 examples that contain JSON null values. Schema validation remains
  # covered by the other OAS rules and the checked-in contract tests.
  duplicated-entry-in-enum: off
  typed-enum: off
