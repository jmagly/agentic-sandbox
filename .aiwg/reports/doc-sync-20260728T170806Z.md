# Doc-Sync Audit — v2026.7.14 release baseline

**Started:** 2026-07-28T16:54:00Z

**Completed:** 2026-07-28T17:08:06Z

**Direction:** code-to-docs

**Scope:** `management`, `agent-rs`, `cli`, `scripts`, `images/qemu`, `docs`,
and `.aiwg`, from the previous completed sync at
`0c09f04276a950ba6783d10ceec51660de0d0d7c` through
`9088f46347d201bd0e891761726f08a330777177`

**Lanes:** API and MCP contracts, runtime and security operations, CI runner
operations, macOS release readiness, changelog and release metadata

## Findings and corrections

1. The `v2026.7.13` announcement and changelog links described an actual tag
   and published release, but neither Gitea nor GitHub has that tag or release.
   The documentation now records `2026.7.13` as a prepared, skipped candidate
   at `086f9ffe2c95c9c6efd9af61d178587d61ac9114`, and states that its work rolls
   into `v2026.7.14`.
2. Apple release-readiness records still named `v2026.7.13` as the active
   candidate. They now identify `v2026.7.14` and retain the hard-stop
   requirement for a new exact-commit credential-free preview, witnessed
   ceremony, and immutable handoff.
3. The public verification guide still described Darwin artifacts as deferred
   even though tag CI now makes the exact witnessed Apple Silicon handoff a
   non-skippable prerequisite for Gitea publication and GitHub mirroring. The
   guide now covers verification of the package, disk image, payload manifest,
   release evidence, checksum manifest, and handoff record.

The read-only MCP adapter has synchronized implementation, operator
documentation, example principal configuration, architecture status, and test
coverage. The admin OpenAPI contract, CA provider protocol, container security
boundary, agentshare retention/storage behavior, Apple package contract, and
build01 CI/KVM runner requirements are likewise represented in their
corresponding documentation. No undocumented field-shape or protocol migration
was found.

## Validation

- `python3 scripts/check-doc-links.py --docs-root docs` — passed
- JSON parsing for `docs/_manifest.json`, `docs/releases/_manifest.json`, and
  `.aiwg/reports/doc-sync-last-run.json` — passed
- `git diff --check` — passed

## Release-flow handoff

The `v2026.7.14` release flow must still:

- bump all three Rust crates and lockfiles from `2026.7.13` to `2026.7.14`;
- create cumulative `v2026.7.14` changelog and announcement content from the
  last published tag, `v2026.7.12`;
- update release manifests/navigation as needed;
- run the configured build/test and CI gates;
- obtain a witnessed macOS handoff bound to the exact release commit and
  `v2026.7.14` before creating the tag.

## Result

Three high-confidence documentation drift findings were corrected. There are
no remaining code-to-docs schema or interface findings. Tag publication remains
blocked by the intentional exact-commit macOS ceremony gate until the release
commit exists and is witnessed.
