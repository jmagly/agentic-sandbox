# Documentation Drift Report

- **Generated:** 2026-05-09T07:13:22Z
- **Direction:** code-to-docs
- **Scope:** incremental — commits since 2026-05-08T19:19:17Z (`6249fe0`, `42e288a`, `effdb43`, `e3dcee1`, `84ed214`)
- **Auditors:** AIWG executor-contract, HTTP API + WS delta

## Summary

| Severity | Count | Auto-fixed | Manual review |
|----------|-------|------------|---------------|
| HIGH     | 13    | 13         | 0             |
| MEDIUM   | 6     | 4          | 2             |
| **Total**| **19**| **17**     | **2**         |

The new surface clusters tightly around the AIWG executor contract (#193).
Approach: created a single `docs/aiwg-executor.md` as the integration's
source of truth, with brief reference stubs from `docs/API.md`,
`docs/ws-protocol.md`, `CLAUDE.md`, and `README.md`.

## Auto-fixed

### `docs/aiwg-executor.md` (NEW, ~250 lines)
Single source of truth covering:
- Configuration (`AIWG_SERVE_ENDPOINT`, `AIWG_SERVE_NAME`, `missions.json`)
- Sandbox + executor dual registration
- Capability vocabulary (6 keys)
- `POST /api/v1/sessions/:id/dispatch` request/response/auth/failure modes
- `DELETE /api/v1/executors/:executor_id`
- Executor WS stream (`/ws/executors/{id}?token=…`)
- Event envelope shape + reference table for all 11 mission/executor events
- Inbound `mission.hitl_responded` injection path
- `MissionState` state machine
- Graceful-shutdown lifecycle (SIGTERM → suspended → reconnect → resumed)
- Status & observability (`/api/v1/aiwg/status`, atomic persistence)
- Known limitations (`mission.progress` not yet wired, local-only resumability, fixed-lifetime tokens)

### `docs/API.md`
- **Auth claim updated** — line 19 previously claimed "no authentication currently required"; now documents the bearer-token exception for the dispatch route.
- **New section** "AIWG executor contract" with `POST /api/v1/sessions/:id/dispatch` request/response/failure shapes, cross-linking to `aiwg-executor.md`.

### `docs/ws-protocol.md`
- **New section** "Executor-contract WS stream (#193)" pointing to `aiwg-executor.md` for the full reference; mentions the second WS endpoint, the bidirectional model, and the parallel-with-sandbox-stream relationship.

### `CLAUDE.md`
- Added "AIWG Executor Contract" section between API Endpoints table and Issue Tracking — one paragraph + link.

### `README.md`
- Extended the existing "aiwg Serve Integration" subsection with a paragraph about the executor-contract layer + link.

## Manual review (MEDIUM, not auto-fixed)

1. **`mission.progress` is reserved but unwired** — emitter constructor exists; no caller. The natural trigger (per-iteration agent runtime signal) needs richer data than the dispatcher exposes. Not an emergent doc fix; needs an issue and design call before docs claim coverage.
2. **`/ws/executors/{id}` is documented from the AIWG-side perspective** (we connect to that URL on AIWG's host). The reverse — sandbox-side incoming WS — doesn't exist. The current docs phrasing is correct but worth a second pass to ensure no reader infers the sandbox accepts this WS.

## Files modified

| File | Status | Lines added |
|------|--------|-------------|
| `docs/aiwg-executor.md` | NEW | ~250 |
| `docs/API.md` | UPDATE | +50 (new dispatch section, auth note) |
| `docs/ws-protocol.md` | UPDATE | +20 (new executor stream pointer) |
| `CLAUDE.md` | UPDATE | +3 |
| `README.md` | UPDATE | +2 |
