# Doc-Sync Audit — PR #664 macOS package contract

**Timestamp:** 2026-07-22T01:56:24Z

**Direction:** code-to-docs

**Scope:** PR #664 (`main...ci/462-macos-package-contract`) plus the inventory of
open pull requests and unmerged local branches

**Lanes:** release packaging, CI/release operations, architecture status,
changelog

## Code surface reviewed

- `scripts/package-macos.sh` adds the Apple Silicon client-tools packaging
  contract: Mach-O checks, hardened-runtime signing, signed Installer package,
  notarization, stapling, Gatekeeper assessment, and checksums.
- `tests/package/test-package-macos.sh` exercises the artifact shape and the
  fail-closed signing/notarization contract with synthetic command fixtures.
- `Makefile` includes that test in `make test-scripts`.
- The production Apple lane remains disabled in `.gitea/workflows/ci.yaml` and
  no tag depends on the preview package.

## Documentation coverage

The PR already synchronizes the public and operator-facing surfaces in:

- `CHANGELOG.md`
- `docs/releases/macos-package.md`
- `docs/releases/runbook.md`
- `docs/architecture/release-pipeline-audit.md`

One additional drift finding was corrected:

| Finding | Severity | Resolution |
|---|---|---|
| `docs/architecture/aarch64-build-runner-plan.md` still described the mutsu Darwin SSH lane as live and named the retired direct `MUTSU_SSH_KEY` interface. | medium | Marked the plan as historical, recorded that the Darwin lane is hard-disabled, linked the current package contract, and documented the Vault-path credential interface used if the lane is re-enabled. |

## Outstanding-work inventory

- PR #664 is the only open pull request and is the delivery candidate for this
  audit.
- Local branch `feat/ch-8-gpu-vfio-655` is superseded by merged PR #657 and the
  follow-up mainline commits that closed #655. Its removed remote and differing
  pre-squash history do not represent outstanding deliverable work.
- Local branch `fix/615-managed-attach-cwd` points at `main` and contains no
  unmerged work.
- Issue #462 intentionally remains open for real Apple-builder signing,
  notarization evidence, and release-lane promotion.

## Validation

- `make test-scripts` — passed
- `bash -n scripts/package-macos.sh tests/package/test-package-macos.sh` — passed
- `shellcheck scripts/package-macos.sh tests/package/test-package-macos.sh` — passed
- `python3 scripts/check-doc-links.py --docs-root docs` — passed
- `git diff --check` — passed
- PR head CI before this documentation correction: conformance, documentation,
  host-runtime, supply-chain, lint, test, build, security, Docker, libvirt E2E,
  and Cloud Hypervisor E2E jobs passed. Tag-only publication jobs did not run on
  the pull request and no credentials or release actions were used.

## Human review boundary

The synthetic contract does not constitute Apple signing or notarization proof.
An eligible Apple builder, Developer ID identities, Keychain notary profile, and
an explicitly approved real proof run remain prerequisites for promoting the
Apple package lane under #462.

## Result

Code and documentation are synchronized for the bounded PR scope. The one
high-confidence drift finding was corrected; no additional implementation issue
is required by this audit.
