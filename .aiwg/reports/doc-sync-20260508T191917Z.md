# Documentation Drift Report

- **Generated:** 2026-05-08T19:19:17Z
- **Direction:** code-to-docs
- **Scope:** full repo
- **Mode:** live (auto-fix HIGH-confidence findings)
- **Auditors run:** 4 (HTTP API, CLI, WS protocol, VM/container provisioning)

## Summary

| Severity | Count | Auto-fixed | Manual review |
|----------|-------|------------|---------------|
| HIGH     | 21    | 14         | 7             |
| MEDIUM   | 10    | 1          | 9             |
| LOW      | 2     | 0          | 2             |
| **Total**| **33**| **15**     | **18**        |

## HIGH Severity Findings

### HTTP API Domain

**Auto-fixed:**
- `[undocumented-endpoint]` 10 routes added to `docs/API.md`: `/agents/{id}/reprovision`, `/agents/{id}/rotate-secret`, `/aiwg/status`, `/aiwg/reconnect`, `/agents/{id}/sessions` POST, `/agents/{id}/sessions/{session}` DELETE (with `?signal=` param), `/container-images`, `/storage/global/_download`, `/storage/inbox/{agent_id}/_download`, `/storage/outbox/{task_id}/_download`.
- `[path-mismatch]` `README.md` claimed `/api/v1/agents/{id}/screen`; actual route is `/api/v1/sessions/{id}/screen` (`management/src/http/server.rs:279`).
- `[method-mismatch]` `docs/API.md` claimed `/healthz` returns plain text `"OK"`; actual response is `Json({"status":"alive"})` (`management/src/http/health.rs`).

### CLI Domain

**Auto-fixed:**
- `[undocumented-command]` Added `container` subcommand group to `docs/cli-design.md` (matches `cli/src/main.rs:138-305`).
- `[flag-mismatch]` Changed `session attach --role observer|controller` → `session attach --write` in `docs/cli-design.md` (matches code at `cli/src/main.rs:359-366`).
- `[stale-command]` Removed `task artifacts get` (code only has `list` — `cli/src/main.rs:416-420`).
- `[stale-command]` Removed `hitl list` and `hitl get` (code only has `respond` — `cli/src/main.rs:538-546`).
- `[stale-command]` Removed `session orchestrate` (no Orchestrate variant in `SessionCommands`).

**Manual review:**
- `[undocumented-command]` `audit-log path` subcommand exists in code (MEDIUM) — judgment call whether it belongs in the design taxonomy or is purely operational.

### WebSocket Protocol Domain

**Auto-fixed:**
- `[undocumented-message]` Added `server_hello` capability-banner message to `docs/ws-protocol.md` (sent as first frame on every WS connection — `management/src/ws/connection.rs:168-172`).
- `[field-mismatch]` Added `Keyframe` to the `session_frame` payload `kind` enumeration in `docs/ws-protocol.md`.

**Manual review (code change required, not doc change):**
- `[capability-mismatch]` `SUPPORTED_CLIENT_MESSAGES` array (`management/src/ws/connection.rs:140-152`) is missing 6 entries that are documented as supported AND handled in code: `ping`, `send_command`, `list_agents`, `create_session`, `detach_session`, `session_resize`. This is a real bug — the server underadvertises its own capabilities, which can cause clients to incorrectly feature-gate. **Fix direction is docs-to-code (update the constant), not code-to-docs.**

### VM/Container Provisioning Domain

**Manual review (substantive doc rewrites or code changes):**
- `[command-example-broken]` `CLAUDE.md:36` mixes legacy `--profile agentic-dev` with modern `--loadout profiles/*.yaml` system without explaining coexistence. Needs a deliberate doc edit to clarify which is preferred.
- `[stale-flag]` `docs/vm-lifecycle.md:99-112` only shows `--profile`; should reference the loadout system.
- `[undocumented-profile]` `provision-vm.sh:103` help text lists only `basic` and `agentic-dev`; 10 additional loadout profiles exist but aren't surfaced. **Code change** — update the script's help text.
- `[image-tag-mismatch]` `README.md:111` shows local Docker tags; CI publishes to `git.integrolabs.net/roctinam/agentic-sandbox/`. Needs a registry section.

## MEDIUM/LOW Findings (manual review)

- HTTP `POST /agents/{id}/sessions` — partially documented, POST verb not explicit
- HTTP `WebSocket /ws/sessions/{id}/orchestrate` — mentioned in code comment, no schema in API.md
- HTTP `POST /agents/{id}/manifests/{platform}/{name}` — paired with GET, POST not explicit
- WS `Keyframe` already covered above
- Provisioning: `--autostart`, `--task-id`, `--mem-limit`, `--cpu-quota`, `--io-weight`, `--io-read-limit`, `--io-write-limit`, `--disk-quota` flags undocumented (provision-vm.sh)
- Container chain: `agentic/agent:base` and `agentic/agent:dev` layer images aren't in any image catalog
- `Dockerfile.management` and `Dockerfile.agent-rust` paths not in deployment docs
- CLI version (`cli/Cargo.toml:3` → `2026.5.0`) not stated in design docs (LOW — convention)

## Files Modified

- `README.md` — path fix for screen endpoint
- `docs/API.md` — added 10 endpoints; fixed /healthz format
- `docs/cli-design.md` — added container group; fixed session attach flag; removed stale `hitl list/get`, `task artifacts get`, `session orchestrate`
- `docs/ws-protocol.md` — added `server_hello`; added `Keyframe` to session_frame kinds

## Files Recommended for Manual Edit

- `CLAUDE.md` — clarify `--profile` vs `--loadout` provisioning
- `docs/vm-lifecycle.md` — add loadout examples
- `images/qemu/provision-vm.sh` — expand `--help` profile list (code change)
- `management/src/ws/connection.rs` — add 6 missing entries to `SUPPORTED_CLIENT_MESSAGES` (code change)
