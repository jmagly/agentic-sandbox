# Doc-Sync Audit — outstanding delivery completion

**Started:** 2026-07-22T02:03:19Z

**Completed:** 2026-07-22T02:12:21Z

**Direction:** code-to-docs

**Scope:** all open Gitea pull requests and unmerged local branches discovered
during the delivery run

**Lanes:** release packaging, CI/release operations, architecture status,
PTY/session integration contract, changelog

## Delivery candidates reviewed

| Candidate | Disposition |
|---|---|
| PR #664, macOS package contract | Reviewed, synchronized, validated, and merged to `main` as `06bceb6`. |
| PR #665, managed attach working directory | Reviewed, synchronized, validated, and merged to `main` as `0c09f04`; #615 closed. |
| `feat/ch-8-gpu-vfio-655` | Not outstanding: superseded by merged PR #657 and later mainline CI/testing-policy commits that closed #655. The remote branch was already removed. |
| `fix/615-managed-attach-cwd` | Delivered by PR #665; no longer outstanding. |

## Findings and corrections

1. `docs/architecture/aarch64-build-runner-plan.md` described the mutsu Darwin
   SSH lane as live and named the retired direct-secret interface. PR #664 now
   marks it as historical, records the hard-disabled lane, links the current
   macOS package contract, and documents the Vault-path interface used if the
   lane is re-enabled.
2. PR #665 changed the observable PTY join fallback but did not document it.
   `docs/contracts/bindings/pty-ws/v1/spec.md` now specifies that an omitted or
   empty join `cwd` inherits the session's effective creation directory, while
   an explicit join value wins. `CHANGELOG.md` records the fix.

No REST, WebSocket, protobuf, OpenAPI, or MCP field shape changed. The #665
update is an implementation correction within the existing `cwd`/`working_dir`
contract, so no schema migration or downstream API issue is required.

## Validation

- `make test-scripts` — passed for PR #664
- macOS package synthetic contract — 3/3 passed
- Cloud Hypervisor backend script checks — 118/118 passed
- fast-start script checks — 67/67 passed
- local VFIO guard/policy checks — 21/21 passed
- `cargo fmt --manifest-path management/Cargo.toml --check` — passed for PR #665
- `cargo test --manifest-path management/Cargo.toml agent_pty_bridge::tests -- --nocapture`
  — 44/44 passed across the library and binary test targets
- final `make check` on PR #665 — passed, including 799 management library
  tests, 835 management binary tests, 165 executor tests, all agent/CLI tests,
  Rust E2E fixtures, and the complete script regression suite
- `python3 scripts/check-doc-links.py --docs-root docs` — passed
- `git diff --check` — passed

PR #664's implementation head passed the full remote CI matrix. Its final
documentation-only head passed the docsite build; teroknor supply-chain retries
failed before checkout because the runner could not pull its standard container
image, while both underlying pin-policy scripts passed locally.
The repeated pre-checkout runner-image failure is tracked as CI incident #666.

## Human review boundary

Issue #462 remains open: Apple release-lane promotion still requires an
eligible Apple builder and explicitly approved real signing/notarization proof.
No credential material or physical GPU operation was used in this run.

## Result

All discovered deliverable branches are merged or proven superseded. There are
no open pull requests, and both documentation drift findings were corrected.
